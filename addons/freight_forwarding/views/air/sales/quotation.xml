<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- ============================================================
         FORM VIEW
         Identik dengan Sea Quotation kecuali:
         - Tidak ada smart button Booking / HBL (sea-specific)
         - Tidak ada container_type (sea-specific)
         - Tidak ada tombol Convert to Booking / Jobsheet (sea-specific)
         - Tab Shipment Info hanya field dari mixin (airport/airline menyusul)
    ============================================================ -->
    <record id="view_air_quotation_form" model="ir.ui.view">
        <field name="name">freight.air.quotation.form</field>
        <field name="model">freight.air.quotation</field>
        <field name="mode">primary</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">

            <xpath expr="//field[@name='partner_id']" position="before">
                <field name="freight_type" />
                <field name="container_type" />
                <field name="validity_date" />
                <field name="quotation_title" />
            </xpath>

            <!-- Fields setelah partner_id: kontak -->
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="email" />
                <field name="phone" />
                <field name="contact_person" />
                <field name="salesman_id" />
            </xpath>

            <!-- Ganti group order_details dengan field custom -->
            <xpath expr="//group[@name='order_details']" position="replace">
                <group>
                    <field name="currency_id" />
                    <field name="currency_rate" />
                    <field name="pricelist_id" />
                    <field name="payment_term_id" />
                    <field name="service_level" />
                    <field name="pricelist_id" readonly="state in ['cancel', 'sale']"
                        groups="!product.group_product_pricelist" />
                    <field name="delivery_type_id" />
                    <field name="effective_date" />
                    <field name="expiry_date" />
                    <field name="reference_number" />
                    <field name="commodity_id" />
                </group>
            </xpath>

            <!-- Tambah grup Address setelah sale_header -->
            <xpath expr="//group[@name='sale_header']" position="after">
                <group string="Address">
                    <group>
                        <span>Source</span>
                        <div class="o_address_format">
                            <field name="source_street" placeholder="Street..."
                                class="o_address_street" />
                            <field name="source_street2" placeholder="Street 2..."
                                class="o_address_street" />
                            <field name="source_city" placeholder="City"
                                class="o_address_city" />
                            <field name="source_state_id" placeholder="State"
                                class="o_address_state" />
                            <field name="source_zip" placeholder="ZIP"
                                class="o_address_zip" />
                            <field name="source_country_id" placeholder="Country"
                                class="o_address_country" />
                        </div>
                    </group>
                    <group>
                        <span>Destination</span>
                        <div class="o_address_format">
                            <field name="destination_street" placeholder="Street..."
                                class="o_address_street" />
                            <field name="destination_street2" placeholder="Street 2..."
                                class="o_address_street" />
                            <field name="destination_city" placeholder="City"
                                class="o_address_city" />
                            <field name="destination_state_id" placeholder="State"
                                class="o_address_state" />
                            <field name="destination_zip" placeholder="ZIP"
                                class="o_address_zip" />
                            <field name="destination_country_id" placeholder="Country"
                                class="o_address_country" />
                        </div>
                    </group>
                </group>
            </xpath>

            <!-- Extra Info di tab Other Information -->
            <xpath expr="//page[@name='other_information']" position="inside">
                <group string="Extra Info">
                    <group>
                        <field name="description_of_goods" />
                        <field name="quantity" />
                        <field name="has_insurance" />
                        <field name="insurance_id" invisible="has_insurance == False" />
                    </group>
                    <group>
                        <field name="actual_weight" />
                        <field name="volume" />
                        <field name="chargeable_weight" />
                    </group>
                </group>
            </xpath>

            <!-- Tab tambahan setelah customer_signature -->
            <xpath expr="//page[@name='customer_signature']" position="after">

                <page string="Dimension">
                    <group>
                        <group>
                            <field name="loose_quantity" />
                            <field name="pcs" />
                            <field name="uom_id" />
                        </group>
                        <group>
                            <field name="length" />
                            <field name="width" />
                            <field name="height" />
                            <field name="dimension" />
                        </group>
                    </group>
                </page>

                <!-- Shipment Info: hanya field dari mixin.
                     Field airport/airline akan ditambahkan di iterasi berikutnya. -->
                <page string="Shipment Info">
                    <group>
                        <group string="Routing Information">
                            <label for="est_transit_time_days" string="Est. Transit Time"/>
                            <div class="o_row">
                                <field name="est_transit_time_days" nolabel="1" class="oe_inline"/>
                                <span class="oe_inline">Days</span>
                            </div>
                            <field name="est_transit_time_note" placeholder="Remark (optional)" />
                            <field name="frequency" />
                            <field name="note" widget="text" />
                        </group>
                        <group string="Commercial Information">
                            <field name="origin_id" />
                            <field name="destination_id" />
                            <field name="frt_collect" />
                        </group>
                    </group>
                </page>

                <page string="Header &amp; Footer">
                    <group>
                        <field name="header" />
                        <field name="special_instruction" />
                        <field name="footer" />
                    </group>
                </page>

                <page string="Terms &amp; Conditions">
                    <group>
                        <field name="terms_and_condition_id" />
                        <field name="description" />
                    </group>
                </page>

                <page string="Cargo Info">
                    <field name="cargo_info_ids" />
                </page>

            </xpath>
        </field>
    </record>

    <!-- ============================================================
         LIST VIEW
    ============================================================ -->
    <record id="view_air_quotation_list" model="ir.ui.view">
        <field name="name">freight.air.quotation.list</field>
        <field name="model">freight.air.quotation</field>
        <field name="mode">primary</field>
        <field name="inherit_id" ref="sale.view_quotation_tree" />
        <field name="arch" type="xml">
            <data />
        </field>
    </record>

    <!-- ============================================================
         SEARCH VIEW
    ============================================================ -->
    <record id="view_air_quotation_search" model="ir.ui.view">
        <field name="name">freight.air.quotation.search</field>
        <field name="model">freight.air.quotation</field>
        <field name="arch" type="xml">
            <search string="Air Quotation">
                <field name="name" string="Quotation Number"/>
                <field name="partner_id"/>
                <field name="user_id" string="Salesperson"/>
                <field name="team_id"/>
                <field name="state"/>
                <field name="freight_type"/>
                <separator/>
                <filter name="my_quotation" string="My Quotations" domain="[('user_id', '=', uid)]"/>
                <filter name="filter_draft_sent" string="Quotation" domain="[('state', 'in', ['draft', 'sent'])]"/>
                <filter name="filter_sale" string="Sales Order" domain="[('state', '=', 'sale')]"/>
                <filter name="filter_cancel" string="Cancelled" domain="[('state', '=', 'cancel')]"/>
            </search>
        </field>
    </record>

    <!-- ============================================================
         WINDOW ACTION
    ============================================================ -->
    <record id="action_freight_air_quotation" model="ir.actions.act_window">
        <field name="name">Air Quotation</field>
        <field name="res_model">freight.air.quotation</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_air_quotation_list"/>
        <field name="views" eval="[(ref('view_air_quotation_list'), 'list'), (ref('view_air_quotation_form'), 'form')]"/>
        <field name="search_view_id" ref="view_air_quotation_search"/>
        <field name="context">{'search_default_my_quotation': 1}</field>
    </record>
</odoo>
